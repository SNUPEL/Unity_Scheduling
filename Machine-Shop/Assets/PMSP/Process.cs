using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//System.Collections and System.Collections.Generic are namespaces that allow one to use collections like ArrayList or List<T>.
//Unity Engine is Unity’s core namespace, containing features like GameObjects, components, physics, etc.

public class Process : MonoBehaviour
//This defines a public class named Process.
//Classes in Unity inherit from MonoBehaviour which is the base class for all Unity scripts and attach them to GameObject and provides lifecycle methods.
{
    // Start() function is called when the script is first enabled, usually at the start of the game or when the GameObject becomes active.
    // Used to initialize variables, set up states, or start coroutines.
    void Start()
    {

    }

    //Update() function is called once per frame. Used for handling frame-dependent logic like user input or movement updates.
    void Update()
    {
        
    }
}
